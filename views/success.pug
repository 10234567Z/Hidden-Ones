extend layout 
block content 
    if currentUser !== undefined 
        p You are now a club member #{currentUser.username}
            a(href="/") Go back home 
    else 
        p Successfully made the account , You now 
            a(href="/") Go home and login with details  